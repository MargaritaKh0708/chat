
import {LoginForm} from 'components/authorization/LoginForm'

export const AuthorizationPage: React.FC = () => {
    return (
        <section className='authorization__page container'>
            <LoginForm/>
        </section>
    )
}